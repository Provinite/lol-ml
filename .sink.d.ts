/**
 * This file is used to make typescript ignore the preact.d.ts file
 * via the `paths` configuration in `tsconfig.json`. Preact has some
 * additions to the `JSX.Element` interface that cause type errors when
 * using react components.
 */
